apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole

metadata:
  name: alloy

rules:
  - apiGroups:
      - ""

    resources:
      - pods
      - pods/log
      - nodes
      - services
      - namespaces

    verbs:
      - get
      - list
      - watch
