kind: ClusterRole

metadata:
  name: promtail

rules:
  - apiGroups:
      - ""

    resources:
      - pods
      - nodes
      - services
      - namespaces

    verbs:
      - get
      - list
      - watch
