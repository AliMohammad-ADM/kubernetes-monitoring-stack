[defaults]

inventory = inventories/production/

roles_path = roles

host_key_checking = false

remote_user = ansible

private_key_file = /home/ansible/.ssh/ansible_key

ass_pass = false

[privilege_escalation]

become = true

become_user = root

become_method = sudo

become_ask_pass = false
